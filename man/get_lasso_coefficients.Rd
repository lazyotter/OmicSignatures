% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signature_utils.R
\name{get_lasso_coefficients}
\alias{get_lasso_coefficients}
\title{Extract Non-Zero Coefficients from a LASSO Model}
\usage{
get_lasso_coefficients(alc_signature, lambda = "lambda.min")
}
\arguments{
\item{alc_signature}{A list object containing the cross-validation model fit,
typically created using the \code{create_signature} function. The cross-validation model
should be stored in the \code{crossval} element.}

\item{lambda}{Character or numeric. The penalty parameter (lambda) at which to
extract the coefficients. Default is \code{"lambda.min"} for the lambda that gives
the minimum cross-validation error.}
}
\value{
A data frame with two columns:
\describe{
\item{\code{Feature}}{The name of the feature (omics variable).}
\item{\code{Coefficient}}{The estimated coefficient for the feature.}
}
The intercept is excluded from the returned data frame.
}
\description{
This function extracts the non-zero coefficients from a LASSO model fit stored
within a signature object. It returns the coefficients along with the feature names
in a data frame format, excluding the intercept.
}
\examples{
# Example usage with a fitted signature object:
# Assuming `alc_signature` is an object created using `create_signature()`
signature <- get_lasso_coefficients(alc_signature, lambda = "lambda.min")
print(signature)

}
